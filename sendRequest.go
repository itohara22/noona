package main

func SendPieceRequest(fileSize int, pieceLength int){

	blockSize := uint32(16384) // fixed 16kb in bytes

	for start := uint32(0); start < uint32(pieceLength); start =+ blockSize{

	}
}
